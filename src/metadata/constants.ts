/**
 * Phase 2D metadata hard production ceilings.
 * Callers may narrow via MetadataOptions; they cannot widen these bounds.
 */
export const MAX_MANIFESTS_PER_METADATA_OPERATION = 128;

export const MAX_MANIFEST_CONTENT_BYTES = 262_144;
