{
  "name": "pathcode-gc1c-live-fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "SYNTHETIC fixture for GC1-c live acceptance only — not canonical suite input",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
